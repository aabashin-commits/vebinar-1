# Лендинг вебинара «Готов ли ваш ребёнок к школе?»

Одностраничный лендинг под вебинар для родителей детей 6 лет (в 1 класс — в сентябре 2027).
Задача страницы: собрать регистрации на эфир и продать годовую программу подготовки
к школе, попутно показав саму школу.

Стилистика унаследована от основного сайта [`../school/`](../school/) — те же цвета,
шрифт, кнопки, скругления и резиновые токены. **Папка полностью автономная:**
свои `assets/`, `css/`, `js/`, никаких ссылок на `../school/`. Её можно залить
куда угодно (поддомен, подпапка), и правки в `school/` её не сломают.

---

## ⚠️ Что нужно сделать перед публикацией

Три вещи ждут вводных от клиента. Пока их нет, лендинг **полностью рабочий**:
кнопки не мёртвые, вёрстка не разъезжается.

| # | Что | Где менять | Статус |
|---|-----|-----------|--------|
| 1 | Реальная дата и время эфира | `js/countdown.js` (константа) **и** JSON-LD в `index.html` | стоит заглушка 17.09.2026, 19:00 МСК |
| 2 | ID CRM-формы Битрикс24 | `index.html`, два фрагмента | форма не подключена, работает fallback |
| 3 | Второй спикер | `index.html`, блок `.speaker--pending` | стоит плейсхолдер-круг |

Подробности по каждому пункту — ниже.

---

## Стек и правила

Те же, что в `school/`:

1. **Чистые HTML + CSS + vanilla JS.** Никаких фреймворков, сборщиков, npm, Tailwind,
   CDN-библиотек. Всё подключается прямо в `index.html`: CSS через `<link>`, JS через `<script defer>`.
2. **Файл на блок.** У каждой секции свой CSS-файл; у блоков с поведением — свой JS.
3. **Цвета, шрифты, радиусы, тени — только через переменные из `css/base.css`.**
   Хардкод хекс-кодов в файлах блоков запрещён.
4. **Медиазапросы блока живут в его же CSS-файле, внизу.** Брейкпоинты — 1280 / 1024 / 767.
5. **⚠️ В `index.html` не оставлять HTML-комментариев** — сборки у проекта нет, файл
   заливается как есть, и комментарии видны в DOM на проде. Все заметки — в этом файле.

---

## Файловая структура

```
vebinar-1/
├── CLAUDE.md            — этот файл (поддерживать актуальным)
├── index.html           — единственная страница
├── css/
│   ├── base.css         — @font-face, CSS-переменные (копия токенов school), reset, типографика
│   ├── layout.css       — .container, кнопки .btn*, .section-card, .pill, .hl, .eyebrow
│   ├── header.css       — липкая шапка + мобильное меню
│   ├── countdown.css    — обратный отсчёт (компонент, стоит дважды)
│   ├── hero.css         — первый экран
│   ├── hook.css         — крючок + сравнение «зачёркнутый чек-лист» / настоящая готовность
│   ├── extremes.css     — «качели» двух крайностей
│   ├── year-path.css    — дорожка года + бары «год против месяца» + опоры программы
│   ├── first-class.css  — синяя секция «что ждёт в 1 классе» + режим дня
│   ├── speakers.css     — карточки спикеров + программа вебинара
│   ├── offer.css        — три бонуса участникам
│   ├── faq.css          — аккордеон
│   ├── final-cta.css    — финальный баннер с регистрацией
│   ├── footer.css       — футер
│   └── mobile-cta.css   — липкая плашка CTA на планшете и телефоне
├── js/
│   ├── main.js          — window.openForm (дефолт), lockScroll/unlockScroll, делегирование CTA
│   ├── header.js        — бургер-меню
│   ├── countdown.js     — ⚠️ ДАТА ЭФИРА + таймер + подстановка даты в разметку
│   ├── faq.js           — данные вопросов + рендер аккордеона
│   ├── mobile-cta.js    — показ липкой плашки после первого экрана
│   └── bitrix.js        — переключение CTA на CRM-форму (когда её подключат)
└── assets/
    ├── fonts/           — Onest variable (woff2, сабсеты cyrillic/latin/latin-ext) + OFL.txt
    ├── favicon/         — набор фавиконок (копия из school)
    └── img/
        ├── logo.svg          — цветное лого (шапка)
        ├── logo_footer.svg   — белое лого (футер)
        ├── green-check.svg   — галочка в списке «настоящая готовность»
        ├── hero/kid.webp     — вырезка девочки в hero (848×1372)
        ├── decor/            — grid.svg, wave.svg, swirl.webp, spark-*.svg, boy.webp
        └── speakers/         — фото спикеров, круглый кроп 400×400
```

Появился новый файл или файл переименован → сразу отразить здесь.

---

## 📅 Как поменять дату вебинара

Дата живёт **в одном месте в коде** — `js/countdown.js`, первая строка внутри модуля:

```js
const WEBINAR_AT = '2026-09-17T19:00:00+03:00';
```

Скрипт сам разносит её по странице: подставляет в шапку, мобильное меню, мета-строку
hero и финальный баннер (узлы `[data-webinar-date]` — короткий формат, `[data-webinar-full]` —
полный с годом и «МСК»), а также считает обратный отсчёт.

**Формат обязателен со смещением зоны `+03:00`** (Москва). Без смещения браузер прочитал бы
время в поясе посетителя, и человек из Новосибирска увидел бы отсчёт до 19:00 по своему
времени. Все даты на странице форматируются через `Intl` с `timeZone: 'Europe/Moscow'`,
поэтому «19:00» означает 19:00 МСК для всех.

⚠️ **Второе место — JSON-LD в конце `index.html`.** Разметка события для поисковиков
(`"startDate"` и `"endDate"`) статична: краулеры надёжнее читают её из HTML, чем из JS.
**Меняя дату, поправьте оба места.** `endDate` = старт + 1 ч 15 мин (доклад + вопросы);
эта же длительность задана в `countdown.js` константой `DURATION_MIN = 75`.

**Три состояния таймера** переключаются сами:
- до эфира — обратный отсчёт с русским склонением («42 дня», «1 минута», «15 минут»);
- эфир идёт (в пределах `DURATION_MIN`) — «Мы в эфире — присоединяйтесь» с пульсирующей точкой;
- эфир прошёл — «Вебинар прошёл. Оставьте заявку — пришлём запись…».

---

## 📝 Как подключить CRM-форму Битрикс24

Сейчас форма **не подключена** — клиент заводит под вебинар отдельную (чтобы лиды
с вебинара не смешивались с обычными заявками школы) и передаёт её ID.

**Пока формы нет** все CTA работают через fallback из `js/main.js`: плавно прокручивают
к секции регистрации `#reg`, где стоят крупная кнопка и телефон школы, и коротко
подсвечивают баннер. Мёртвых кнопок на странице нет.

**Когда клиент даст ID** (он выглядит как `click/225/yn061r`, где `225` — номер формы):

1. В `index.html` сразу после `<body>` добавить лоадер — рядом со скрытой кнопкой-триггером:

   ```html
   <script data-b24-form="click/NNN/xxxxxx" data-skip-moving="true">(function(w,d,u){var s=d.createElement('script');s.async=true;s.src=u+'?'+(Date.now()/180000|0);var h=d.getElementsByTagName('script')[0];h.parentNode.insertBefore(s,h);})(window,document,'https://cdn-ru.bitrix24.ru/bXXXXXXX/crm/form/loader_NNN.js');</script>
   ```

2. Тот же `data-b24-form` прописать на кнопке-триггере, которая уже есть в разметке:

   ```html
   <button class="b24-trigger" type="button" data-b24-form="click/NNN/xxxxxx" aria-hidden="true" tabindex="-1"></button>
   ```

Больше ничего менять не нужно. `js/bitrix.js` увидит лоадер и переопределит `window.openForm`
на клик по скрытому триггеру — все CTA страницы начнут открывать CRM-форму.
**Если лоадера нет, `bitrix.js` молча ничего не делает** и остаётся fallback: именно поэтому
лендинг рабочий и без формы.

Как это устроено: все кнопки на странице помечены `[data-open-form]`, обработчик один —
делегированный, в `main.js`. Разметке кнопок про Битрикс знать не нужно.

---

## 👥 Спикеры

**Спикер 1 — Плесецкая Олеся Васильевна.** Данные взяты из блока «Обучайтесь комфортно»
основного сайта (`../school/js/staff.js`, категория «руководящий состав»), фото —
`../school/assets/img/staff/plesetskaya.webp`, скопировано в `assets/img/speakers/`.

**Спикер 2 — плейсхолдер.** В `index.html` это блок `.speaker.speaker--pending`: вместо
фото стоит круг `--color-light-blue` со знаком вопроса, чтобы раскладка секции была готова.
Чтобы подставить реального человека:

1. положить фото в `assets/img/speakers/<slug>.webp` — **круглый кроп 400×400**,
   «голова и плечи» (как у `plesetskaya.webp`);
2. заменить `<p class="speaker__photo speaker__photo--placeholder">?</p>` на
   `<img class="speaker__photo" src="assets/img/speakers/<slug>.webp" alt="Имя Фамилия" width="400" height="400" loading="lazy">`;
3. заполнить `.speaker__name`, бейджи и тексты; убрать модификатор `speaker--pending`.

Формат карточки — тот же компонент, что на сайте школы: первый бейдж «solid» (синяя
заливка) — роль, остальные голубые — факты вроде стажа. В текстах основное серое
(`--color-gray-2`), выделенное возвращается в чёрный через `<span class="speaker__accent">`.

⚠️ **В ТЗ клиента ведущими названы Мураковская А. С. и Добрынина В. В.** — эти имена
на лендинге не используются: источником правды выбраны карточки, которые присылает
клиент (решение 06.08.2026). Если имена всё же нужны — уточнить у клиента, кто ведёт.

---

## 📊 Аналитика

**Счётчиков на странице нет** — по решению от 06.08.2026. Если понадобятся, снипетты
Яндекс.Метрики (`37734395`) и Roistat можно скопировать из `../school/index.html`,
они вставляются сразу после `<body>`. Вставлять их — решение клиента: это отправка
данных о посетителях во внешние сервисы.

---

## Структура страницы (порядок = порядок секций)

| # | Секция | id | Что делает |
|---|--------|-----|-----------|
| 0 | Шапка | — | Лого, дата эфира, 4 якоря, CTA. Бургер с 1024 |
| 1 | Hero | `#hero` | Синяя панель: H1, мета эфира, таймер, CTA, вырезка ребёнка |
| 2 | Крючок | `#about` | Цитата + сравнение: перечёркнутый чек-лист против настоящей готовности |
| 3 | Две крайности | `#extremes` | «Качели»: слишком рано / само дозреет, между ними пилла |
| 4 | Год против месяца | `#year-path` | Дорожка учебного года, бары 9 месяцев vs 6 недель, 4 опоры, врезка о маленькой группе |
| 5 | 1 класс | `#first-class` | Синяя секция: бенто фактов о программе + режим дня цветными плашками |
| 6 | Кто ведёт | `#speakers` | Карточки спикеров + программа вебинара (липкая колонка) |
| 7 | Оффер | `#offer` | Диагностика бесплатно / первый месяц 50% / набор с 12.09.2026 |
| 8 | FAQ | `#faq` | Аккордеон, 6 вопросов (данные в `js/faq.js`) |
| 9 | Финальный CTA | `#reg` | Баннер с обещанием, второй таймер, кнопка, телефон |
| — | Футер | — | Лого, якоря, контакты, реквизиты |

Три визуальные метафоры, на которых держится страница: **зачёркнутый чек-лист** (секция 2),
**качели крайностей** (секция 3) и **год против месяца** (секция 4). Они несут основную
аргументацию — если менять, менять осознанно.

---

## Конвенции

### CSS
- Классы БЭМ-подобные с префиксом блока: `.hero__title`, `.faq__item--open`.
- Резиновая модель как в school: кегли — `clamp()` по контрольным точкам 1200 → 1600 → 1920,
  геометрия и декор — `cqw` от `.container` (у него `container-type: inline-size`).
- Вертикальный ритм между секциями — `--section-gap`, задан один раз в `layout.css`
  правилом `main > section { margin-top: … }`. Секции сами отступы не задают.
- ⚠️ **Подсветка `.hl` рисуется с вертикальным padding.** В заголовке с плотным
  `line-height` (у h1/h2 базовый 0.9) плашка наезжает на строку выше. Поэтому у
  `.final-cta__title` стоит `1.2`, у `.hero__subtitle` — `1.25`. Добавляете `.hl`
  в новый заголовок — проверьте, что строки не перекрываются.

### Тексты
Нормы те же, что в school: ёфикация, кавычки-«ёлочки», тире `—` в предложении,
диапазоны через `–` («40–50 минут», «9:00–16:00»), неразрывные пробелы после коротких
предлогов и союзов, между числом и словом («6 лет», «1 класс»).

⚠️ **Где какой символ:**
- в `index.html` — сущность `&nbsp;`;
- **в данных JS (`faq.js`, сообщения таймера) — литеральный U+00A0**, а не сущность:
  строки вставляются через `textContent`, и `&nbsp;` вывелась бы видимым текстом.
  В редакторе такой пробел неотличим от обычного — в шапке `faq.js` стоит предупреждение.

### JS
- Vanilla, ES2015+, без зависимостей. Один файл = один блок, каждый самодостаточен:
  не нашёл свой корневой элемент — тихо вышел.
- Глобальные только явные хелперы из `main.js`: `openForm`, `lockScroll`, `unlockScroll`.
- Порядок подключения важен: `main.js` задаёт дефолтный `openForm`, `bitrix.js` грузится
  последним и может его переопределить.

---

## Проверка

Локальный сервер (открывать файл напрямую нельзя — шрифты и `fetch` упрутся в CORS):

```bash
cd "/Users/administrator/Documents/Lendosi/vebinar-1" && python3 -m http.server 8765
```

Что проверять после правок:

1. **Ширины 1920 / 1440 / 1280 / 1024 / 768 / 375** — нет горизонтальной прокрутки
   (`document.documentElement.scrollWidth === window.innerWidth`), ничего не наезжает.
2. **Таймер** — тикает; чтобы проверить два других состояния, временно подставьте
   в `WEBINAR_AT` время 10 минут назад («эфир идёт») и вчерашнее («прошёл»).
3. **CTA** — без лоадера Битрикса клик прокручивает к `#reg` и подсвечивает баннер.
4. **FAQ** — открывается/закрывается, одновременно открыт только один пункт,
   `aria-expanded` меняется.
5. **Бургер (≤1024)** — открывается, закрывается по клику на якорь, страница под ним
   не прокручивается (`body.no-scroll`).
6. **Консоль** — без ошибок и 404.

ℹ️ Предупреждение Chromium «resource was preloaded but not used» про `.woff2` —
ложное: шрифты грузятся ровно по одному запросу и применяются (проверено).

---

## Деплой

Настроенного FTP у этой папки **нет** (в отличие от `school/`, где заливкой занимается
плагин VSCode SFTP по `.vscode/sftp.json`). Куда публиковать — уточнить у клиента:

- **вариант «подпапка»** — `https://agb.lancmanschool.com/vebinar-1/`; именно этот URL
  сейчас прописан в `canonical`, `og:url` и JSON-LD;
- **вариант «поддомен»** — тогда заменить домен во всех абсолютных URL: `canonical`,
  `og:url`, `location.url` и `offers.url` в JSON-LD.

⚠️ **Имя папки — `vebinar-1`, через дефис.** С пробелом URL превратился бы
в `/vebinar%201/` — некрасиво в выдаче и ломает часть почтовых клиентов при вставке ссылки.

⚠️ Основной сайт стоит за DDoS-Guard, статика кешируется на сутки. Если лендинг ляжет
на тот же домен, после заливки правки CSS/JS могут не появиться сразу — проверять
с `?v=N` в URL или ждать сброса кеша.
