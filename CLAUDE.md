# Лендинг вебинара «Готов ли ваш ребёнок к школе?»

Одностраничный лендинг под вебинар для родителей детей 6 лет (в 1 класс — в сентябре
2027). Задача страницы: собрать регистрации на эфир и продать годовую программу
подготовки к школе, попутно показав саму школу.

Стилистика унаследована от основного сайта [`../скул офлайн/`](../скул%20офлайн/) —
те же цвета, шрифт, кнопки, скругления и резиновые токены. **Папка полностью
автономная:** свои `assets/`, `css/`, `js/`, никаких ссылок на соседей. Её можно залить
куда угодно (поддомен, подпапка), и правки в основном сайте её не сломают.

ℹ️ Рядом лежит [`../vebinar-2/`](../vebinar-2/) — второй вебинар, сделанный копией
этой папки. Часть файлов у них совпадает байт в байт, см. «Что важно не сломать».

**Подробная документация** (детали, замеры, грабли — там, здесь только суть):

| Файл | О чём |
|---|---|
| [README.md](README.md) | Обзор проекта и запуск |
| [docs/structure.md](docs/structure.md) | Полное дерево файлов с пояснениями |
| [docs/sections.md](docs/sections.md) | Структура страницы: порядок и логика секций |
| [docs/integrations.md](docs/integrations.md) | Дата эфира, CRM-форма Битрикс24, аналитика |
| [docs/content.md](docs/content.md) | Спикеры: кто, откуда фото, чего не путать |
| [docs/conventions.md](docs/conventions.md) | CSS, поведение вырезки по ширинам, тексты, JS |
| [docs/testing.md](docs/testing.md) | Что и как проверять после правок |
| [docs/deploy.md](docs/deploy.md) | Куда публиковать, имя папки, кеш DDoS-Guard |

---

## ⚠️ Что нужно сделать перед публикацией

Три вещи ждут вводных от клиента. Пока их нет, лендинг **полностью рабочий**:
кнопки не мёртвые, вёрстка не разъезжается.

| # | Что | Где менять | Статус |
|---|-----|-----------|--------|
| 1 | Реальная дата и время эфира | `js/countdown.js` (константа) **и** JSON-LD в `index.html` | заглушка 17.09.2026, 19:00 МСК |
| 2 | ID CRM-формы Битрикс24 | `index.html`, два фрагмента | форма не подключена, работает fallback |
| 3 | Фото Добрыниной В. | `assets/img/speakers/`, `index.html` | пока стоит фото Мураковской (решение клиента) |

⚠️ **Форма нужна отдельная от обычных заявок школы** — иначе лиды с вебинара
смешаются с общей воронкой.

Подробности по каждому пункту — [docs/integrations.md](docs/integrations.md)
и [docs/content.md](docs/content.md).

---

## Стек и правила

Те же, что в `скул офлайн/`:

1. **Чистые HTML + CSS + vanilla JS.** Никаких фреймворков, сборщиков, npm, Tailwind,
   CDN-библиотек. Всё подключается прямо в `index.html`: CSS через `<link>`,
   JS через `<script defer>`.
2. **Файл на блок.** У каждой секции свой CSS-файл; у блоков с поведением — свой JS.
3. **Цвета, шрифты, радиусы, тени — только через переменные из `css/base.css`.**
   Хардкод хекс-кодов в файлах блоков запрещён.
4. **Медиазапросы блока живут в его же CSS-файле, внизу.** Брейкпоинты — 1280 / 1024 / 767.
5. ⚠️ **В `index.html` не оставлять HTML-комментариев** — сборки у проекта нет, файл
   заливается как есть, и комментарии видны в DOM на проде. Все заметки — в md-файлах.

---

## Структура папки (коротко)

```
vebinar-1/
├── CLAUDE.md  README.md  docs/   — документация
├── index.html                    — единственная страница
├── css/   base.css → layout.css → по файлу на секцию
├── js/    main.js · header.js · countdown.js · faq.js · mobile-cta.js · bitrix.js
└── assets/  fonts (Onest) · favicon · img (hero, decor, speakers)
```

Полное дерево с пояснениями по каждому файлу — [docs/structure.md](docs/structure.md).
**Новый файл появился / переименован → сразу отразить там.**

---

## Секции страницы (порядок = порядок секций)

| # | Секция | id | Что делает |
|---|--------|-----|-----------|
| 0 | Шапка | — | Лого, дата эфира, 4 якоря, телефон, CTA. Бургер с 1024 |
| 1 | Hero | `#hero` | Синяя панель: H1, мета эфира, таймер, CTA, вырезка ребёнка |
| 2 | Кто ведёт | `#speakers` | Карточки спикеров + программа вебинара `#agenda` |
| 3 | Крючок | `#about` | Цитата + сравнение: список с крестиками против списка с галочками |
| 4 | Две крайности | `#extremes` | Две карточки (слишком рано / само дозреет) + строка-вывод |
| 5 | Программа подготовки | `#year-path` | 4 опоры программы + врезка о маленькой группе |
| 6 | 1 класс | `#first-class` | Синяя секция: бенто фактов + режим дня цветными плашками |
| 7 | Оффер | `#offer` | Диагностика бесплатно / первый месяц 50% / набор с 12.09.2026 |
| 8 | FAQ | `#faq` | Аккордеон, 6 вопросов (данные в `js/faq.js`) |
| 9 | Финальный CTA | `#reg` | Баннер с обещанием, второй таймер, кнопка, телефон |
| — | Футер | — | Лого, якоря, контакты, реквизиты |

Основную аргументацию несут два блока-сравнения: **крестики против галочек**
(секция 3) и **две крайности** (секция 4). Если менять, менять осознанно.

История переездов и отменённых вариантов — [docs/sections.md](docs/sections.md).

---

## Что важно не сломать

- ⚠️ **Из секции 5 удалены дорожка по месяцам и сравнение сроков (06.08.2026).**
  Дорожка обещала календарный график («в ноябре — внимание, в январе —
  самостоятельность»), которого **нет в ТЗ клиента**. Возвращать такой график
  можно только после согласования со школой: это обещание, за которое кто-то отвечает.
- ⚠️ **Наклон карточек в «Двух крайностях» убран (18.08.2026, просьба клиента.)**
  Модификаторы `--early` / `--late` остались в разметке как семантические хуки,
  CSS-правил у них больше нет.
- ⚠️ **`speakers.css` совпадает с `../vebinar-2/` байт в байт**, а
  `.extremes__middle-text` продублирован там как `.choice__middle-text` —
  правки держать синхронными.
- ⚠️ **Ведущие сменились 18.08.2026: Казакова Людмила и Добрынина Валерия.**
  Имена — без отчеств, ровно как прислал клиент. Под именем Казаковой намеренно
  оставлен текст карточки Плесецкой, а под именем Добрыниной — фото Мураковской
  (`murakovskaya.webp`): и то и другое — прямые решения клиента, а не недоделка.
  Прежде чем «исправлять», прочитайте [docs/content.md](docs/content.md).

---

## Конвенции (суть)

- Классы БЭМ-подобные с префиксом блока: `.hero__title`, `.faq__item--open`.
- Резиновая модель: кегли — `clamp()` по контрольным точкам 1200 → 1600 → 1920,
  геометрия и декор — `cqw` от `.container`.
- Вертикальный ритм между секциями — `--section-gap`, задан один раз в `layout.css`.
  Секции сами отступы не задают.
- Полупрозрачная белая плашка на синем — **только через `--fill-on-accent`**.
- ⚠️ **Растровые вырезки масштабируем по МЕНЬШЕЙ стороне и держим запас под Retina:**
  `CSS-размер × 2 ≤ натуральный размер файла`, иначе картинка пикселит.
- Ручные `<br>` в заголовках карточек не ставим — для ровных строк `text-wrap: balance`.
- **Тексты:** ёфикация, кавычки-«ёлочки», тире `—`, диапазоны `–`, неразрывные пробелы
  после коротких предлогов и между числом и словом.
  ⚠️ В `index.html` — сущность `&nbsp;`; **в данных JS — литеральный U+00A0**
  (строки вставляются через `textContent`).
- **JS:** vanilla ES2015+, один файл = один блок, каждый самодостаточен. Порядок
  подключения важен: `main.js` задаёт дефолтный `openForm`, `bitrix.js` грузится
  последним и может его переопределить.

Полностью, с замерами и поведением вырезки по ширинам —
[docs/conventions.md](docs/conventions.md).

---

## Проверка

Локальный сервер (открывать файл напрямую нельзя — шрифты и `fetch` упрутся в CORS):

```bash
cd "/Users/administrator/Documents/Lendosi/vebinar-1" && python3 -m http.server 8765
```

Короткий чек-лист: ширины **1920 / 1440 / 1280 / 1024 / 768 / 375** без горизонтальной
прокрутки · таймер во всех трёх состояниях · CTA прокручивает к `#reg` и подсвечивает
баннер · FAQ (одновременно открыт один пункт, `aria-expanded` меняется) ·
бургер ≤1024 · консоль без ошибок и 404.

Полный список — [docs/testing.md](docs/testing.md).

---

## Деплой

Настроенного FTP у папки **нет**. Сейчас во всех абсолютных URL прописан вариант
«подпапка» — `https://agb.lancmanschool.com/vebinar-1/`. Уйдёт на поддомен — заменить
домен в `canonical`, `og:url`, `location.url` и `offers.url` в JSON-LD.

⚠️ **Имя папки — `vebinar-1`, через дефис.** ⚠️ Основной сайт стоит за DDoS-Guard,
статика кешируется на сутки — проверять с `?v=N` в URL.

Детали — [docs/deploy.md](docs/deploy.md).
