# Конвенции: CSS, вырезка по ширинам, тексты, JS

## CSS
- Классы БЭМ-подобные с префиксом блока: `.hero__title`, `.faq__item--open`.
- Резиновая модель как в school: кегли — `clamp()` по контрольным точкам 1200 → 1600 → 1920,
  геометрия и декор — `cqw` от `.container` (у него `container-type: inline-size`).
- Вертикальный ритм между секциями — `--section-gap`, задан один раз в `layout.css`
  правилом `main > section { margin-top: … }`. Секции сами отступы не задают.
- ⚠️ **Иконкам мета-строки hero нужен `--color-blue-chip`, а не `--color-blue-2`.**
  Плашка пункта — это 15% белого поверх светлой части градиента, в сумме
  примерно `(132, 176, 244)`. Замер контраста к этому фону: `--color-blue-2`
  давал **1.10:1** (иконки сливались, жалоба клиента 11.08.2026),
  `--color-blue` — 2.15, `--color-blue-chip` — **2.84**. Норма для графики 3:1,
  до неё не дотягивает ни один цвет палитры, но 2.84 против 1.10 — это разница
  между «не видно» и «читается». Хардкод более тёмного синего в обход палитры
  не заводить: цвет должен меняться вместе с ней.
- ⚠️ **Таймер hero на телефоне тянется через `align-self: stretch` у `.hero__timer`.**
  Одного `flex: 1` у ячеек (оно есть в `countdown.css`) недостаточно:
  у `.hero__content` стоит `align-items: flex-start`, блок таймера сжимался
  по содержимому, и растягивать ячейкам было нечего. Замер на 375: сетка 315px
  во всю ширину контента, четыре ячейки по 76px.

- ⚠️ **Полупрозрачная белая плашка на синем — только через `--fill-on-accent`.**
  Один токен на три места: ячейки таймера (`countdown.css`), пункты мета-строки
  hero (`hero.css`) и пилла `.pill--ghost` (`layout.css`). Заведён 11.08.2026,
  когда клиент попросил у пунктов мета-строки «заливку как у таймера»: до этого
  одно и то же `rgba(255, 255, 255, 0.15)` было скопировано по файлам, и связь
  держалась только на памяти. Теперь «как у таймера» верно по построению.
  ⚠️ Не путать с `rgba(255, 255, 255, 0.12)` в карточках синих секций —
  там намеренно другая, более слабая заливка.

- ⚠️ **Подсветка `.hl` рисуется с вертикальным padding.** В заголовке с плотным
  `line-height` (у h1/h2 базовый 0.9) плашка наезжает на строку выше. Поэтому у
  `.final-cta__title` стоит `1.2`, у `.hero__subtitle` — `1.25`. Добавляете `.hl`
  в новый заголовок — проверьте, что строки не перекрываются.
- ⚠️ **Растровые вырезки масштабируем по МЕНЬШЕЙ стороне и держим запас под Retina.**
  На экране с DPR 2 браузеру нужно вдвое больше физических пикселей, чем занимает
  картинка в CSS. Правило: `CSS-размер × 2 ≤ натуральный размер файла`, иначе
  картинка «пикселит». На этом уже спотыкались: `.hero__kid` был привязан к ширине
  колонки и вырастал до 906px по высоте при 1372px в файле — апскейл 1.32×.
  Сейчас у него `height` с потолком 700px (= 1400 физических против 1372).
## 📱 Поведение вырезки по ширинам (правки клиента 11.08.2026)

Раньше вырезка ниже 1024 уходила в фон на `opacity: 0.28`, а на телефоне
пряталась совсем. Сейчас так:

| Ширина | Раскладка | Вырезка |
|--------|-----------|---------|
| >1024 | две колонки | по высоте панели, прижата к низу, поля слева/справа равны |
| 768–1024 | **две колонки** | **непрозрачная**, размер задаёт ширина колонки |
| ≤767 | одна колонка | **в потоке под кнопками**, по центру, текст на всю ширину |

⚠️ **На 768–1024 размер задаёт ШИРИНА колонки, а не высота панели.**
С `height: 100%` вырезка считала бы высоту от панели и по ширине вылезала бы
за свою колонку — на узком планшете это наезд на текст. `width: 100%` от колонки
гарантирует, что она всегда помещается, а высота едет пропорционально.
Доля колонки там `0.5fr`, а не десктопная: на 768 текстовой колонке нужно
остаться шире заголовка и сетки таймера (замер на 768: колонка 452px,
таймер 285px — влезает).

⚠️ **На ≤767 у `.hero__media` обязательны `justify-self: stretch` и сброс `margin`.**
В базовом правиле стоят `justify-self: center` и два отрицательных поля
(сдвиг вправо + вынос за нижний padding). В одноколоночной раскладке они увели бы
фигуру вбок и за край панели, а при `center` блок сжимался бы по содержимому
и `width: 100%` у картинки считался бы не от панели.
Нижний padding панели на телефоне обнулён — фигура садится на кромку, как на десктопе.

Замеры после правки — вебинар 2: 1024 → 331×416 непрозрачная, текст 623px;
768 → 246×309, текст 452px; 375 → 315×396 под кнопками, отступ 40px, по центру,
низ на кромке. Вебинар 1: 1024 → 334×540; 768 → 249×402; 375 → 315×510.
Горизонтальной прокрутки нет нигде, апскейла нет нигде.

- ⚠️ **Ручные `<br>` в заголовках карточек не ставим.** Они рассчитаны на одну
  ширину и при изменении раскладки рвут заголовок на короткие строки. Для ровных
  строк — `text-wrap: balance` (см. `.extremes__card-title`). Исключение —
  крупные заголовки секций, где перенос выверен по композиции.
- ⚠️ **Вывод между карточками (`.extremes__middle-text`) — голый текст кеглем h2,
  а не плашка.** За 18.08.2026 он прошёл три правки клиента: розовая пилла →
  белый прямоугольник → просто текст. Компонент `.pill` из разметки убран
  целиком, класс переименован из `__middle-pill`. Что важно не сломать назад:
  **заливку не возвращать** (на кегле h2 она читалась бы как кнопка размером
  с заголовок) и **`white-space: nowrap` не восстанавливать** — на 375
  `--fs-h2` это 32px, и длинная строка вебинара 2 физически не помещается
  в экран, перенос обязан оставаться за браузером.
  Тот же элемент с теми же значениями есть в `../vebinar-2/css/choice.css`
  (`.choice__middle-text`) — правки держать синхронными.

## Тексты
Нормы те же, что в school: ёфикация, кавычки-«ёлочки», тире `—` в предложении,
диапазоны через `–` («40–50 минут», «9:00–16:00»), неразрывные пробелы после коротких
предлогов и союзов, между числом и словом («6 лет», «1 класс»).

⚠️ **Где какой символ:**
- в `index.html` — сущность `&nbsp;`;
- **в данных JS (`faq.js`, сообщения таймера) — литеральный U+00A0**, а не сущность:
  строки вставляются через `textContent`, и `&nbsp;` вывелась бы видимым текстом.
  В редакторе такой пробел неотличим от обычного — в шапке `faq.js` стоит предупреждение.

## JS
- Vanilla, ES2015+, без зависимостей. Один файл = один блок, каждый самодостаточен:
  не нашёл свой корневой элемент — тихо вышел.
- Глобальные только явные хелперы из `main.js`: `openForm`, `lockScroll`, `unlockScroll`.
- Порядок подключения важен: `main.js` задаёт дефолтный `openForm`, `bitrix.js` грузится
  последним и может его переопределить.


---

← [CLAUDE.md](../CLAUDE.md) · [README.md](../README.md)
